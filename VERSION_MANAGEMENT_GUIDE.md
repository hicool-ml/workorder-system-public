# 系统版本管理指南

## 概述

本系统现已集成完整的版本管理功能，允许管理员在系统设置中管理版本号、发布日期和版本历史记录。

## 功能特性

### 1. 版本信息管理
- **版本号设置**: 支持语义化版本号（如 2.0.0）
- **发布日期管理**: 记录每个版本的发布日期
- **版本说明**: 为每个版本添加详细的发布说明

### 2. 版本历史记录
- **自动记录**: 每次版本更新都会自动保存版本说明
- **历史查看**: 可以查看所有历史版本的发布说明
- **时间戳**: 记录版本创建和更新的时间

### 3. 系统集成
- **全局访问**: 通过 SystemHelper 类全局访问版本信息
- **前端显示**: 在系统设置页面显示当前版本信息
- **API支持**: 提供版本信息的API接口

## 使用方法

### 1. 访问版本管理

1. 使用管理员账户登录系统
2. 在左侧菜单中点击"系统设置"
3. 在版本管理区域可以看到当前版本信息

### 2. 更新版本

1. 在系统设置页面，点击"更新版本"按钮
2. 填写新版本号（如 2.1.0）
3. 选择发布日期
4. 可选：填写版本发布说明
5. 点击"更新版本"保存

### 3. 查看版本历史

1. 在系统设置页面，点击"版本历史"按钮
2. 系统将显示所有历史版本的发布说明
3. 再次点击"版本历史"按钮可以隐藏历史记录

## 技术实现

### 1. 数据库结构

版本相关信息存储在 `system_settings` 表中：

- `system_version`: 系统版本号
- `system_release_date`: 系统发布日期
- `version_notes_{version}`: 版本发布说明（如 `version_notes_2_0_0`）

### 2. 模型方法

#### SystemSetting 模型
```php
// 获取版本号
SystemSetting::get('system_version')

// 设置版本号
SystemSetting::set('system_version', '2.0.0', 'string', '系统版本号', true)
```

#### SystemHelper 类
```php
// 获取系统版本
SystemHelper::getSystemVersion() // 返回 "v2.0.0"

// 获取发布日期
SystemHelper::getSystemReleaseDate() // 返回 "2025-12-16"
```

### 3. 控制器方法

#### SystemSettingController
- `updateVersion()`: 更新版本信息
- `getVersionHistory()`: 获取版本历史记录

### 4. 路由配置

```php
// 更新版本
Route::post('system-settings/update-version', [SystemSettingController::class, 'updateVersion']);

// 获取版本历史
Route::get('system-settings/version-history', [SystemSettingController::class, 'getVersionHistory']);
```

## 版本号规范

建议使用语义化版本号（Semantic Versioning）：

- **主版本号**: 不兼容的API修改
- **次版本号**: 向下兼容的功能性新增
- **修订号**: 向下兼容的问题修正

示例：
- `2.0.0` - 主要版本更新
- `2.1.0` - 新功能添加
- `2.1.1` - 问题修复

## 初始设置

系统已预置了 2.0.0 版本的信息：

- 版本号: 2.0.0
- 发布日期: 2025-12-16
- 版本说明: 优化登录页面显示，避免重复系统名称；将第二个系统名称改为"系统登录"；提升用户体验和界面一致性；添加标准版本管理功能到系统设置模块

## 注意事项

1. **权限要求**: 只有管理员角色可以更新版本信息
2. **版本格式**: 建议遵循语义化版本号规范
3. **版本说明**: 建议详细记录每个版本的主要变更
4. **数据备份**: 在进行重大版本更新前建议备份数据

## 测试

可以使用提供的测试脚本验证版本管理功能：

```bash
php test_version_management.php
```

该脚本将测试：
- 版本信息获取
- 版本更新功能
- 版本历史记录
- 数据一致性

## 未来扩展

可能的未来功能扩展：
1. 版本标签和分类
2. 自动版本检测
3. 版本比较和升级路径
4. 版本回滚功能
5. 版本发布自动化