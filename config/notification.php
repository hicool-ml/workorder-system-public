<?php

return [

    /*
    |--------------------------------------------------------------------------
    | 通知队列化
    |--------------------------------------------------------------------------
    |
    | 启用后工单事件通知（站内信 + 短信/企微/钉钉/飞书 HTTP 调用）改为队列异步
    | 发送，避免创建/解决工单等操作被外部网络超时拖慢。
    |
    | 注意：启用前提是部署环境持续运行 queue:work（Docker 镜像已通过
    | supervisor 内置；本地开发需 composer run dev 或手动 php artisan queue:work）。
    | 未运行 worker 时通知会滞留 jobs 表，直到 worker 启动后才发送。
    |
    */

    'queue_enabled' => env('NOTIFY_QUEUE', false),

];
